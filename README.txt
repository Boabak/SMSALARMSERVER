This skecth end is :
1-When start as first time , start an AP using open SSID as YASAP so user can connect to it via android phone and send some data to it using an android app,
the data contains the number of sim800 SIM card, The number of ADMIN USER to recieve SMSs and send some sort of orders and two other numbers to receive TXT sms, one password for that open SSID, 

2- When user submit that data, THE ESP8266 12 saves all into EEPROM then restarts after restart SSID password has been changed to the password user entered. 